import React, { useState } from "react";

import { SlButton } from "@shoelace-style/shoelace/dist/react";

const Button = () => (
  <>
    <SlButton className="orange" size="large" href="https://google.com/">
      Button
    </SlButton>
  </>
);

export default Button;
